using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoonController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //updates bool in animator
        //gameObject.GetComponent<Animator>().SetBool("MoonDown", GameObject.Find("GameController").GetComponent<DetectLight>().isMoodyLight);
    }
}
